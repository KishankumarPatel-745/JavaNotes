package com.flowControl.scjp;

public class Java02 {
    public static void main(String[] args) {
        /*
        Selection Statements
        1, if-else
        //b is always be boolean type

        if(boolean b){
            //action if b is true
        }else{
            //action if b is false
        }

        eg.
        The argument to the if statement should be boolean type. By mistack if we are trying to provide any other type, then we will get compile time error.

        eg.
        int x=0;
        if(x){
            sop("Hello");
        }else{
            sop("Hi");
        }

        CTE: Incompatible type 
        found:int
        required:boolean

        eg.
        int x=10;
        if(x=20){
            //Same Error...
            sop()
        }

        eg.
        int x=10;
        if(x==20){
            sop("hello");
        }else{
            sop("hi");
        }
        o/p :- hi

        eg.
        int x=true;
        if(x=false){
            sop("hello");
        }else{
            sop("hi");
        }
        o/p :- hi

        eg.
        int x=false;
        if(x==false){
            sop("hello");
        }else{
            sop("hi");
        }
        o/p :- hello


        else part and curly braces are option without curly braces only one statement is allow under if,which should not be declarative statement.
        eg.
        if(true)
            sop("hello");

        eg.
        if(true);

        eg.
        if(true)
            int x=10;
        NOt allowed CTE


        eg.
        if(true){
            int x=10;
        }
        Allowed correct

        Note:-
        ; semicolon is a valid java statement,which is also known as empty statement.

        Note:
        There is no dangling else problem in Java.
        Every else is matched/mapped to the nearest if statement.

        

        




        */
    }
}
